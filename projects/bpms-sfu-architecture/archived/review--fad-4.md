| Severity | Location | Issue | Why it matters | Recommended action | Gate impact |
|---|---|---|---|---|---|
| blocker | §4.0 / §4.2.1 / §4.5.2 | `clk_dsp = clk_rfdc_axis / 3` and RFDC↔DSP “not CDC” are presented as facts, but no pinned accepted ADR/source proves this topology. Parent docs only state recovered/system clock drives FPGA logic and ADC/DAC converters, not this RFDC→MMCM derivation. | This directly determines clock grouping, timing analysis, CDC closure, and RFDC wrapper contract. A wrong synchronous assumption creates unsafe unconstrained CDC or impossible timing. | Mark as `[ASSUMPTION]` or `[INFERRED]` until ADR-0002/ADR-0007 is accepted; keep RFDC↔DSP CDC as TBD/conditional until topology is baselined. | G1 |
| major | §4.3.4 | “Aurora link drops” during discipline-source switchover contradicts §4.3.3/parent mode behavior where primary→secondary is possible and the SFU drives OBG UL in secondary mode. It is also more specific than sourced. | Clock-mode switchover behavior drives reset, alarms, and link recovery requirements. Unsupported “link drops” language will leak into MS reset contracts. | Replace with `[TBD: switchover impact on Aurora link, RTL Designer/HW Lead]`; do not assert drop/hitless behavior unless sourced. | G1 |
| major | §4.4.1 / §4.4.4 | “NCO phase accumulators are not reset” is asserted as a hard architectural rule. For Band Doppler and CW beacon, phase/reset behavior affects deterministic latency, spectral transients, and 1PPS apply semantics. | This is external behavior, not just implementation style. Reset/no-reset of phase state must be specified per module or explicitly TBD. | Move NCO phase-reset policy to affected MS contracts or mark `[TBD: phase state reset/apply semantics, Architect]`. | later G4a |
| major | §4.4.6 / §4.5.4 | `obg_link` FIFO behavior says DSP-side read pointer reset while Aurora-side write pointer preserved during sample-rate switch. That is implementation-specific and risks invalid async FIFO state unless formally specified. | Reset-domain crossing and async FIFO pointer reset asymmetry is a CDC correctness hazard. | At FAD level specify only architectural drop/flush requirement; move pointer behavior to `obg_link` uArch, or require full FIFO flush/reinitialization contract in MS. | G1 / CDC |
| major | §4.2.6 | The example XDC uses `create_clock` on 1PPS and then `set_false_path -from [get_ports pps_in]`. 1PPS is correctly described elsewhere as an asynchronous event, not a timing clock. | This can mislead constraints authoring and produce meaningless clock objects/exceptions. | Remove `create_clock` for 1PPS from the illustrative constraints; specify asynchronous input synchronization at `pps_capture` only. | constraints |
| major | §4.5.3 CDC-03/04 | Multi-bit Aurora status/control fields use “two-flop synchroniser per bit” with no quasi-static contract or handshake. §4.5.1 itself forbids undocumented multi-bit two-flop synchronization. | Per-bit sync of changing multi-bit fields can create incoherent sampled status/control words. | Split into single-bit quasi-static signals, or specify handshake/snapshot protocol for packed multi-bit fields. | G1 / CDC |
| major | §4.5.3 CDC-08 | TLE-derived NCO word is 32-bit and crosses mgmt→dsp by handshake, but “gated by 1PPS edge in `clk_dsp`” is underspecified for atomic multi-register updates across sub-bands/directions. | Band Doppler Local Autonomous apply must be coherent at 1PPS. Partial updates create band mismatch. | Add atomic shadow/active update contract at FAD level; leave storage implementation to MS/uArch. | G1 / later G4a |
| major | §4.3.5 | Inter-SFU phase coherence is described as “inherent” in primary mode. Parent docs also say residual skew arises from optical fibre and requires TBD skew/trim definition. | “Inherent” understates an open system timing risk and can hide required compensation/measurement. | Change to “common source is inherent; residual skew budget/trim remains TBD” and keep OQ tied to SYS-CLK-09/OI-S02. | G1 |
| major | §4.2.3 | `clk_mgmt` source from PS `pl_clk0`, PS boot ordering, and “first clock available” are asserted without source/marker. | PS/PL partitioning is explicitly pending ADR-0003; this becomes a hidden architecture decision. | Mark PS `pl_clk0` management-clock source as `[ASSUMPTION]` or defer to ADR-0003/ADR-0002. | G1 |
| minor | §4.1 | `clk_dsp` FPGA-sat value is rounded to 349.525 MHz. Exact 1,048.576/3 = 349.525333… MHz. | Rounding is acceptable for prose but poor for constraints/budget seeds. | Use 349.525333 MHz or state rounded display value explicitly. | constraints |
| minor | §4.1 | `clk_aurora_user` approximate values are computed from line rate/interface width but omit encoding/word-rate assumptions. | The numbers may be misleading before Aurora IP configuration. | Keep only `[TBD: depends on Aurora IP user-interface width, RTL Designer]` or label calculations as rough examples. | G1 |
| minor | §4.4.5 | Power-on sequence gives “order of seconds” without source/marker. | Startup timing becomes a factual performance claim. | Mark as `[TBD: startup timing budget, owner]` or remove estimate. | G1 |
| question | §4.6.2 | Step S3 says reprogram LMK/LMX reference output, while §4.3.2 says sample-rate switching reprograms only RFdc tile PLL, not LMK/LMX discipline source. | Internal contradiction in the switch sequence. | Decide whether LMK/LMX output frequency changes during sample-rate switch; update §4.3.2 or §4.6.2 consistently. | G1 |

## Gate assessment

- **G1 status:** blocked
- **Blocking items:**
  1. Unsupported RFDC→DSP synchronous-derived topology.
  2. Pending ADR-0006/0007 treated as accepted.
  3. Filter-bank clock/CDC contradiction against SFU-001.
- **ADR triggers:** ADR-0002 required for clock architecture; ADR-0003 for PS/PL management-clock partitioning; ADR-0006/0007 must be accepted or all dependent claims marked pending.
- **Do-not-change:** 1PPS treated as timing event, not data clock; `obg_link` owns Aurora↔DSP CDC at contract level; primary/secondary mode distinction; explicit approved CDC mechanism list; no FIFO depth in FAD.